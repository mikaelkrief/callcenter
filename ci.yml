steps:
- task: DevMikaelKrief.githubtools.GitHubTag.GitHubTag@0
  inputs:
    githubEndpoint: "github connection OAuth"
    tag: "v$(Build.BuildNumber)"
    repositoryName: "$(Build.Repository.Name)"
    commmit: "$(Build.SourceVersion)"
